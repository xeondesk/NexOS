{
  "name": "nexos",
  "version": "0.1.0",
  "description": "NexOS — self-hosted development environment orchestration (supervisor, log/exec control plane, metrics, editor+terminal services, framework runtime hooks)",
  "private": true,
  "type": "commonjs",
  "license": "MIT",
  "bin": {
    "nexos": "./bin/nexos"
  },
  "scripts": {
    "test": "bash tests/supervisor-smoke.sh && node tests/log-proxy-smoke.mjs && bash tests/metrics-smoke.sh && bash tests/bridge-smoke.sh && node tests/editor-extension-smoke.mjs && bash tests/vsix-smoke.sh && bash tests/sign-server-smoke.sh && bash tests/web-smoke.sh && bash tests/api-smoke.sh && bash tests/api-crud-smoke.sh && bash tests/api-stream-smoke.sh && bash tests/api-meta-smoke.sh && bash tests/api-preview-smoke.sh"
  },
  "dependencies": {
    "jsondiffpatch": "^0.7.6",
    "ws": "^8.19.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@v0-sdk/react": "^3.0.3",
    "ai": "^7.0.56",
    "v0": "^3.0.3"
  }
}
